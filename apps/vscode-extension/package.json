{
  "name": "@contextforge/vscode-extension",
  "version": "0.0.0",
  "private": true,
  "description": "ContextForge VS Code Extension — Interface principale",
  "type": "module",
  "main": "./dist/extension.js",
  "scripts": {
    "build": "tsc",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "clean": "rimraf dist"
  },
  "dependencies": {
    "@contextforge/contracts": "workspace:*",
    "@contextforge/core": "workspace:*",
    "@contextforge/configuration": "workspace:*"
  },
  "devDependencies": {
    "rimraf": "^5.0.0"
  }
}
