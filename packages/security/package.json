{
  "name": "@contextforge/security",
  "version": "0.0.0",
  "private": true,
  "description": "ContextForge Security — Détection de secrets, blocage de fichiers sensibles",
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "clean": "rimraf dist"
  },
  "dependencies": {
    "@contextforge/contracts": "workspace:*",
    "@contextforge/core": "workspace:^",
    "@contextforge/shared": "workspace:*"
  },
  "devDependencies": {
    "rimraf": "^5.0.0"
  }
}
