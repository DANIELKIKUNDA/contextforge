{
  "name": "@contextforge/shared",
  "version": "0.0.0",
  "private": true,
  "description": "ContextForge Shared — Utilitaires transverses génériques (Result, horloge, IDs, chemins, tri stable)",
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "clean": "rimraf dist"
  },
  "devDependencies": {
    "rimraf": "^5.0.0"
  }
}
