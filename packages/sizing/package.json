{
  "name": "@contextforge/sizing",
  "version": "0.0.0",
  "private": true,
  "description": "ContextForge Sizing — Calcul de tailles, estimation de tokens",
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "clean": "rimraf dist"
  },
  "dependencies": {
    "@contextforge/contracts": "workspace:*",
    "@contextforge/shared": "workspace:*"
  },
  "devDependencies": {
    "rimraf": "^5.0.0"
  }
}
